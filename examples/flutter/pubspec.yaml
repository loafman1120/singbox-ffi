name: singbox_ffi_example
description: Dart FFI smoke examples for singbox-ffi.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.3
  singbox_ffi:
    path: ../..
